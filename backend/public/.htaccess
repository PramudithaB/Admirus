<IfModule mod_rewrite.c>
    RewriteEngine On

    # If file or folder exists → serve it
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Otherwise → load React index.html (inside public)
    RewriteRule ^ index.html [L]
</IfModule>
